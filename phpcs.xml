<?xml version="1.0"?>
<ruleset name="Better Media Manager">
	<description>WordPress Coding Standards for Better Media Manager</description>

	<!-- Check all PHP files -->
	<arg name="extensions" value="php"/>
	
	<!-- Show progress and use colors -->
	<arg value="ps"/>
	<arg name="colors"/>
	
	<!-- Parallel processing -->
	<arg name="parallel" value="8"/>

	<!-- What to scan -->
	<file>.</file>
	
	<!-- Exclude patterns -->
	<exclude-pattern>*/vendor/*</exclude-pattern>
	<exclude-pattern>*/node_modules/*</exclude-pattern>
	<exclude-pattern>*/.git/*</exclude-pattern>

	<!-- Use WordPress Coding Standards -->
	<rule ref="WordPress">
		<!-- Allow short array syntax -->
		<exclude name="Generic.Arrays.DisallowShortArraySyntax"/>
		
		<!-- We prefer Yoda conditions but don't enforce -->
		<exclude name="WordPress.PHP.YodaConditions.NotYoda"/>
	</rule>

	<!-- Check for PHP cross-version compatibility -->
	<config name="testVersion" value="7.4-"/>
	<rule ref="PHPCompatibilityWP"/>

	<!-- WordPress-Extra rules -->
	<rule ref="WordPress-Extra"/>

	<!-- WordPress-Docs rules -->
	<rule ref="WordPress-Docs"/>

	<!-- Verify text domain -->
	<rule ref="WordPress.WP.I18n">
		<properties>
			<property name="text_domain" type="array">
				<element value="better-media-manager"/>
			</property>
		</properties>
	</rule>

	<!-- Verify that everything in the global namespace is prefixed -->
	<rule ref="WordPress.NamingConventions.PrefixAllGlobals">
		<properties>
			<property name="prefixes" type="array">
				<element value="better_media_manager"/>
				<element value="Better_Media_Manager"/>
			</property>
		</properties>
	</rule>

	<!-- Minimum WordPress version -->
	<config name="minimum_supported_wp_version" value="5.8"/>

	<!-- Allow files without strict types declaration -->
	<rule ref="PSR12.Files.FileHeader.SpacingAfterBlock">
		<severity>0</severity>
	</rule>
</ruleset>
